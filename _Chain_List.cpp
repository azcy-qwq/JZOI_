#include<bits/stdc++.h>
using namespace std;
template<typename T>
class __Chain_List{
    protected:
        vector<T> data;
        int *head,*next;
    public:
        void add(T x){
            data.push_back(x);
        }
};
int main(){
//ios::sync_with_stdio(0);
    
}